# قوانین معماری Electron

- کد renderer نمی‌تواند مستقیماً به سیستم‌فایل دسترسی داشته باشد.
- Preload تنها پل بین renderer و Electron main است.
- منطق بازیابی و فهرس‌سازی در ماژول‌های سرویس زندگی می‌کند، نه در اجزای رابط‌کاربری.
- Log‌کردن باید ساختار‌یافته باشد و از مرزهای سرویس صادر شود.
