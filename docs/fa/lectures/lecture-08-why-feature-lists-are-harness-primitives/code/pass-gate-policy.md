# سیاست درگاه برابری

یک ویژگی فقط می‌تواند از `passes: false` به `passes: true` حرکت کند هنگام‌که:

- جریان‌ی کار انتظارشده اجرا شده است
- شواهد موفقیت ثبت‌شده است
- هیچ خطای مسدود‌کننده در مسیر آزمایش‌شده‌ی حاضر نیست
- پیاده‌سازی برنامه را در وضعیت شکسته یا ابهام‌آمیز بر نمی‌گذارد
